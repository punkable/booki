# WinUI 3 experiment (retired)

The experimental native WinUI 3 dock (`src-winui/`) was removed. Booki ships as
the Tauri + WebView2 app at the repository root.

Historical design notes that still apply to the current product live in
[`ARCHITECTURE.md`](./ARCHITECTURE.md).
