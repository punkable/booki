<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Booki_Dock.DockWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Booki_Dock.Models"
    Title="Booki Dock">
    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Border
        x:Name="DockSurface"
        Padding="8"
        Background="{ThemeResource LayerFillColorAltBrush}"
        BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="12">
        <Grid ColumnSpacing="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ListView
            x:Name="PinnedList"
            IsItemClickEnabled="True"
            ItemClick="PinnedList_ItemClick"
            SelectionMode="None">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:PinnedItem">
                    <Button
                        Width="52"
                        Height="52"
                        Margin="2"
                        AutomationProperties.Name="{x:Bind Name}"
                        Background="Transparent"
                        CornerRadius="8"
                        ToolTipService.ToolTip="{x:Bind Name}">
                        <Button.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Click="OpenItem_Click" Tag="{x:Bind}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE8A7;" />
                                    </MenuFlyoutItem.Icon>
                                    Abrir
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Click="RemoveItem_Click" Tag="{x:Bind}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE74D;" />
                                    </MenuFlyoutItem.Icon>
                                    Quitar del dock
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Button.ContextFlyout>
                        <Grid>
                            <Border
                                Width="38"
                                Height="38"
                                Background="{ThemeResource AccentFillColorDefaultBrush}"
                                CornerRadius="8" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="16"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                Text="{x:Bind Initial}" />
                        </Grid>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Grid.Column="1" Width="42" Height="52" Click="Settings_Click"
                Background="Transparent" ToolTipService.ToolTip="Ajustes">
            <FontIcon Glyph="&#xE713;" />
        </Button>
        </Grid>
    </Border>
</Window>
